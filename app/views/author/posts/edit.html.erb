<% provide(:page_title, "Edit") %>
<% provide(:author, 'active') %>

<h1>Editing Post</h1>

<%= render 'form', post: @post=Post.friendly.find(params[:id])%>

<%= link_to 'Show', @post %> |
<%= link_to 'Back', author_posts_path %>

